# @moriajs/cli

The command-line interface for MoriaJS.

## Commands

- `moria init`: Scaffold a new project.
- `moria dev`: Start the development server.
- `moria build`: Build for production.

## Installation

```bash
npm install -g @moriajs/cli
```
